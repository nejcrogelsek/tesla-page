import React from "react";
import Header from "./components/Header";

const App = () => {
  return <div>
    <Header />
    {/* items => snapping */}
    {/* items => snapping */}
    {/* items => snapping */}
    {/* items => snapping */}
  </div>;
};

export default App;
